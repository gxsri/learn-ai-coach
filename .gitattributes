# 统一换行符，避免 Windows / macOS / Linux 协作者之间产生"整文件都改了"的假 diff
* text=auto

# Windows 脚本必须保留 CRLF：
#  · .cmd / .bat 在 LF-only 下可能被 cmd.exe 误解析
#  · .ps1 用 CRLF 最稳
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# 这些是二进制或不该被改动的内容
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.mp4  binary
*.pt   binary

# 让 GitHub 的 linguist 不要把这些当成主要语言来统计
research/**  linguist-documentation
part1-多agent协作/**  linguist-documentation
part2-YOLO与骨架/**   linguist-documentation
